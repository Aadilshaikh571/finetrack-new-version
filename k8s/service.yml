apiVersion: v1
kind: Service
metadata:
  name: finetrack-service
  namespace: finetrack
spec:
  selector:
    app: finetrack-frontend
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30080
  type: NodePort
